/**
 *  set of safe defaults to initialize variables
 *  Freeze is used to avoid unintended mutations
 *  */

exports.EMPTY_OBJECT_READONLY = Object.freeze({});

exports.EMPTY_ARRAY_READONLY = Object.freeze([]);
